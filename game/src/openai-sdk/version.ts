/**
 * NPC version.
 * Update this value to re-create the NPC assistants.
 * NPC assistants are special prompts that are capable to retrieve information from specific files -- it's RAG, but from withinthe OpenAI interface.
 */
export const version = 6;
